import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const AccountDetailScreen = () => {
    return (
        <View>
            <Text>AccountDetail Screen</Text>
        </View>
    );
};

const styles = StyleSheet.create({

});

export default AccountDetailScreen;