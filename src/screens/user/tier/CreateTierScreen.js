import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const CreateTierScreen = () => {
    return (
        <View>
            <Text>CreateTier Screen</Text>
        </View>
    );
};

const styles = StyleSheet.create({

});

export default CreateTierScreen;